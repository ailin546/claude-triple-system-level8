# Shared State

Multi-agent coordination directory.

- `board.json` — Global task board (runtime, gitignored)
- `decisions.log` — Agent decision audit trail (runtime, gitignored)
- `artifacts/` — Shared build artifacts between agents
